# Crawl_Compositions_Scrapy
Crawl compositions based on Scrapy Framework for learning only

# package
scrapy
pymysql

You can use these commends to install packages:

pip3 install scrapy

pip3 install pymysql
